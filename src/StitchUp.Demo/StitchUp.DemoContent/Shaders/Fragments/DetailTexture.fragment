interface()
{
	$name = detail_texture
	$textures = detail_map
	$vertex = uv
	$interpolators = uv
	$uv = 2
}

ps 1_1

void main(INPUT input, inout OUTPUT output)
{
	output.color.rgb *= tex2D(detail_map, input.uv) * 2;
}