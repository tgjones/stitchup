fragment PixelColorOutput;

[ps]
__hlsl__
void main(INPUT input, inout OUTPUT output)
{
	// Add light colours together.
	float4 totalLightColor = float4(0, 0, 0, 0);
	import(_LightColor, totalLightColor += _LightColor);
	output.color = totalLightColor;
}
__hlsl__